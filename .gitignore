# Dependencies
node_modules

# Build outputs (Vite)
dist
dist-ssr

# Vite / local overrides
*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment & secrets (keep .env.example if you add one)
.env
.env.*
!.env.example

# Whisper / Tauri large local assets (use `npm run setup:whisper`)
src-tauri/binaries/whisper-cli-*.*
src-tauri/binaries/whisper-server-*.*
src-tauri/binaries/_whisper_cpp_unpack/
src-tauri/binaries/_whisper_cpp_build/
src-tauri/binaries/whisper-cpp-v*.tar.gz
src-tauri/binaries/.whisper_darwin_setup_tag
src-tauri/resources/models/*.bin

# Rust / Tauri — release & debug artifacts, deps cache
src-tauri/target/

# Tauri CLI / generated (if present)
src-tauri/gen/

# IDE
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Tests / coverage
coverage
*.lcov

# TypeScript
*.tsbuildinfo
