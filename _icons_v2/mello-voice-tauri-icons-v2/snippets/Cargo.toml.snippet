# Cargo.toml
# Add the image-png feature because the runtime icon switching snippet uses PNG icons.
# Keep your existing features and add "image-png".

[dependencies]
tauri = { version = "2", features = ["image-png"] }
